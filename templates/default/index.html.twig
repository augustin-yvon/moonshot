{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}


{% block body %}
    <div class="">
        {% if app.user %}
            <h1>Bienvenu {{ app.user.firstname }}</h1>
        {% else %}
            <h1>Bienvenu</h1>
        {% endif %}

        <div>{{ dump(app.user) }}</div>
    </div>
{% endblock %}
